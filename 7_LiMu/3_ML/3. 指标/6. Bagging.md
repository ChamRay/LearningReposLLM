
# Bagging - Booststrap AGGrgratING
- Bagging平行训练n个基础模型,每个模型是独立的
- 对于回归问题,每个模型的输出做平均;对于分类问题,则多个模型进行投票
- 每个模型通过bootstrap采样的数据集来训练
    - 可放回的随机采样m个样本
    - 大约(1-1/e) 63%的样本会被采样到,剩下的则可作为验证集

# 随机森林
- 使用决策树作为基座模型
- 通常随机选择这些模型的特征子集,可以避免一些过拟合

# 不稳定模型
- bagging 减少的是方差,尤其是对不稳定的模型
- 决策树不是很稳定,线性回归是比较稳定的
