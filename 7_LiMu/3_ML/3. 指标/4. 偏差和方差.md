
在统计学习中,通常使用方差和偏差衡量一个模型
在多轮学习中
1. 如果模型学习到的点和真实模型之间位移(距离)比较小,则是低偏差;而学习点间的距离比较近,则表示低方差
2. 如果学习到的点和真实模型之间的位移比较大,没有落在训练范围,则表示为高偏差;但每个点比较近,则方差比较小
3. 学习的点落在区域内,偏差比较小,但点之间距离比较大,则方差大
4. 学习的点即不落在模型区域,也相差比较大,则偏差大,方差大

# 偏差和方差的定义
- 偏差:
- 方差:

# 减小方差&偏差
- 减小偏差:需要更复杂的模型
- 减小方差:需要更简单的模型
- 减小误差:需要更多优质数据
